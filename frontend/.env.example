# Copier ce fichier en .env.local pour le développement

# URL de l'API backend (utilisée pour les endpoints publics uniquement)
# En dev, laisser vide — Vite proxifie /api vers localhost:8000
# En production : VITE_API_URL=https://mon-hotel-api.railway.app
VITE_API_URL=
